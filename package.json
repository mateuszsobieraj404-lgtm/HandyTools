{
  "name": "handytools",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "server": "node --env-file-if-exists=server/.env server/server.js",
    "test": "node --test src/time.test.js"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
